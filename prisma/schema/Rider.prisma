model Rider {
    id     String @id @default(uuid())
    user   User   @relation(fields: [userId], references: [id])
    userId String

    order Order[]

    createdAt DateTime @db.Timestamptz()
    updateAt  DateTime @db.Timestamptz()

    @@index([id, userId])
    @@map("rider")
}
