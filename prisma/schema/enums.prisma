enum ROLE {
    admin
    customer
    provider
    rider
}

enum ORDER_STATUS {
    PREPARING
    READY
    ON_THE_WAY
    DELIVERED
    CANCLLED
}

enum STATUS {
    PENDING
    APPROVED
    BLOCKED
    CANCLE
}
